#pragma once

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <iostream>

class EntityHandler {
public:
    // TODO: make this code do what its supposed to, temporary code
    static int spawnEntity();

    static int currentId;
};
